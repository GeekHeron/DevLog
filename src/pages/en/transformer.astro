---
import BaseLayout from '../../layouts/BaseLayout.astro';
import head from '../../sources/en/transformer.head.html?raw';
import body from '../../sources/en/transformer.body.html?raw';

const headerProps = { variant: 'en', hrefPrefix: '../', aboutHref: 'about-en.html' };
const htmlProps = { lang: "en" };
---
<BaseLayout htmlProps={htmlProps} head={head} body={body} headerProps={headerProps} />
