---
import BaseLayout from '../layouts/BaseLayout.astro';
import head from '../sources/embodied-ai.head.html?raw';
import body from '../sources/embodied-ai.body.html?raw';

const headerProps = {  };
const htmlProps = { lang: "zh-CN" };
---
<BaseLayout htmlProps={htmlProps} head={head} body={body} headerProps={headerProps} />
