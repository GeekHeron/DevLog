---
import BaseLayout from '../layouts/BaseLayout.astro';
import head from '../sources/global-yield.head.html?raw';
import body from '../sources/global-yield.body.html?raw';

const headerProps = {  };
const htmlProps = { lang: "zh-CN" };
---
<BaseLayout htmlProps={htmlProps} head={head} body={body} headerProps={headerProps} />
