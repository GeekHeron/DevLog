---
import BaseLayout from '../layouts/BaseLayout.astro';
import head from '../sources/openclaw_arch.head.html?raw';
import body from '../sources/openclaw_arch.body.html?raw';

const headerProps = {  };
const htmlProps = { lang: "zh-CN" };
---
<BaseLayout htmlProps={htmlProps} head={head} body={body} headerProps={headerProps} />
