---
import BaseLayout from '../layouts/BaseLayout.astro';
import head from '../sources/fuxi-protocol.head.html?raw';
import body from '../sources/fuxi-protocol.body.html?raw';

const headerProps = {  };
const htmlProps = { lang: "zh-CN" };
---
<BaseLayout htmlProps={htmlProps} head={head} body={body} headerProps={headerProps} />
