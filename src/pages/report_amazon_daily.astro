---
import BaseLayout from '../layouts/BaseLayout.astro';
import head from '../sources/report_amazon_daily.head.html?raw';
import body from '../sources/report_amazon_daily.body.html?raw';

const headerProps = {  };
const htmlProps = { lang: "zh-CN" };
---
<BaseLayout htmlProps={htmlProps} head={head} body={body} headerProps={headerProps} />
