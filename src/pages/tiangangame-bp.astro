---
import BaseLayout from '../layouts/BaseLayout.astro';
import head from '../sources/tiangangame-bp.head.html?raw';
import body from '../sources/tiangangame-bp.body.html?raw';

const headerProps = { chrome: 'none', chromeBlank: 0 };
const htmlProps = { lang: "zh-CN" };
---
<BaseLayout htmlProps={htmlProps} head={head} body={body} headerProps={headerProps} />
