---
import BaseLayout from '../layouts/BaseLayout.astro';
import head from '../sources/article-agent-workflow.head.html?raw';
import body from '../sources/article-agent-workflow.body.html?raw';

const headerProps = {  };
const htmlProps = { lang: "zh-CN" };
---
<BaseLayout htmlProps={htmlProps} head={head} body={body} headerProps={headerProps} />
