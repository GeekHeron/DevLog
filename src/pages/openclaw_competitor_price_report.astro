---
import BaseLayout from '../layouts/BaseLayout.astro';
import head from '../sources/openclaw_competitor_price_report.head.html?raw';
import body from '../sources/openclaw_competitor_price_report.body.html?raw';

const headerProps = {  };
const htmlProps = { lang: "zh-CN" };
---
<BaseLayout htmlProps={htmlProps} head={head} body={body} headerProps={headerProps} />
