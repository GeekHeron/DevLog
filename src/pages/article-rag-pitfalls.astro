---
import BaseLayout from '../layouts/BaseLayout.astro';
import head from '../sources/article-rag-pitfalls.head.html?raw';
import body from '../sources/article-rag-pitfalls.body.html?raw';

const headerProps = { leadBlank: 1, chromeBlank: 0 };
const htmlProps = { lang: "zh-CN" };
---
<BaseLayout htmlProps={htmlProps} head={head} body={body} headerProps={headerProps} />
