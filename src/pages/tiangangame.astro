---
import BaseLayout from '../layouts/BaseLayout.astro';
import head from '../sources/tiangangame.head.html?raw';
import body from '../sources/tiangangame.body.html?raw';

const headerProps = { leadBlank: 1 };
const htmlProps = { lang: "zh-CN" };
---
<BaseLayout htmlProps={htmlProps} head={head} body={body} headerProps={headerProps} />
